export const isEmpty=t=>(t=t.trim(),""===t||null==t?"":t);export const toEELISASeparator=t=>t.includes(",")?t.split(",").map(t=>t.trim()).join(" / "):t.trim();export const wordLimit=t=>{const o=t.split(" ");return o.length>=18?o.slice(0,18).join(" ").concat(" ..."):t};